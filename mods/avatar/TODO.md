# Enigma Avatar — BACKLOG / KNOWN ISSUES / TODO
**Authoritative pending list. READ THIS before any avatar work so nothing gets missed.**

## LIVE BACKLOG (genuinely open -- start here)
**Reconciled against the code 2026-06-26 (doc-vs-code audit, 3 agents).** The lettered sections (A-F)
and audit blocks below are kept for their detail but are HISTORY -- many of their `[ ]` items are in
fact DONE (recorded in the DONE batches above / the HISTORY LOG below). This bucketed list is the
canonical "what is actually open." Code is in good shape: no dead code, no stubs, no unwired actions.

**AUDIT 2026-06-26 (trust-nothing, 4 agents).** Session changes audited CLEAN. Real defects found + FIXED:
remote-URL block bypass via leading-whitespace/embedded-tab (`loader.js`, test-pinned); bus `lookAt`
non-finite coords froze cursor-look (`avatar.js`); spring CONSTRUCTOR `regionWeight` bypassed the 0..2
clamp (`spring.js`, test-pinned); panic/quit hotkeys registered without checking success (`main.js`, now
logs a dead key). Lower-severity items left open are in the buckets below (H1 re-arbitration, tar-slip)
or noted as minor: drag 48px watchdog deadzone, stale `_overByWin` on single-window reload,
`verify:"where"` mouse false-positive, name-tier center-role drop when the sole center bone is side-tagged.

### 1) Needs the live overlay + real models (cannot be judged from code/tests)
- [ ] **FEEL TUNING by the user's eye.** Motion amplitude/choreography; the velocity-clamp vs Filian
  snappiness (co-speech head wiggle is rate-limited to the head's `speed_limit`); jaw axis/sign
  (`facialTune`) + lip-sync gain. Headless tests can't judge feel; software WebGL can't render skinned
  meshes -- drive the live overlay + `EnigmaAvatar.snap()`.
- [ ] **Model-zoo stragglers** (`node tools/rig_report.mjs <model>`, confirm the swing live): lolbit/
  mangle arm recovery (+mangle hips/chest) via a cascade-tier improvement; grace_howard needs a UTF-8
  bone-name re-export (baked-in `U+FFFD` mojibake). Plus the user-input questions in section B (makiro
  hair, "reversed bones", "some parts don't show") -- need the user to say WHICH model/part.

### 2) Fixable now (small, code-only)
- [ ] **(SAFETY) No periodic main-side re-arbitration** (`main.js` `applyInteractive` is event-driven only).
  A *hung* renderer latched at `over:true` keeps capturing clicks over her footprint until the user hits
  the panic key / tray (no auto self-heal). Add a periodic `applyInteractive()` (or a renderer-heartbeat ->
  force-through) on the existing 4s topmost-reassert timer. Also clear the sender's `_overByWin` entry on
  `render-process-gone`/reload so a reused webContents id can't keep a stale `true`.
- [ ] **Spring verlet only PARTIALLY dt-normalized** -- gravity is dt-scaled (`spring.js`); inertia +
  the stiffness pull are still frame-count-based. Normalize the rest for frame-rate independence.
- [ ] **Folder rename renames only the manifest LABEL, not the on-disk folder** (`library.js`
  `renameModel`, comment at its head). A true rename must migrate the folder/URL + the profile key.
- [ ] **Click-through guard has no test.** The silhouette hit-test (`computeOver`/`overSilhouette`,
  avatar.js) + `containsEvent` (ui.js) are safety-critical and untested -- needs the logic extracted
  to be unit-testable headlessly. `dom.js` mock also omits `importDropped`/`setInteractive`.
- [ ] **No speech bubble** for spoken lines (none in any source).
- [ ] **Multi-mesh divergent-morph UI** + morph/lip-sync index collision -- only the bounded
  "primary group" morph path exists (Low).

### 3) The brain (P4) -- PARTIAL (ties to the 2026-06-26 deep-research)
- [ ] The bus + `perform`/`pose`/verify path and `brain.py` are BUILT (capabilities->author->
  verify-by-numbers loop; deterministic grounded author + optional `--llm`). OPEN: the persistent,
  perception/memory-driven "mind" (event loop, conversation integration) and Enigma-as-author once it
  finishes pretraining. Architecture decision pending the deep-research report.

### 4) Deferred by design (bigger -- do NOT start without an explicit call)
- [ ] Real physics engine (rigid/cloth/constraints) + boneless cloth (detail in section E below).
- [ ] Sit-on-chair / throw-ball / embodied agent (needs physics + IK; IK was removed in the lean pass).
- [ ] Clip retargeting / CC0 clip pack -- **SUPERSEDED by the lean pass** (primitives-only); do not
  re-adopt authored clips without reversing that decision. VRM fast-paths (`vrm.lookAt`/VRMA) stay valid.
- [ ] Wander behavior; outfit SWAP beyond mesh show/hide; "sleep when idle" deeper power toggle; a
  nicer/"cuter" no-model placeholder (today: inert marker + ASCII hint -- do NOT self-author a character).

### 5) Environment / housekeeping
- [ ] **`BALL_URL` asset missing** from this checkout (`props/worn_baseball_ball/...glb`) -> conjure/
  throw have nothing to render here; conjure already fails HONESTLY via `onMiss`. Stage the .glb (it
  lives in the standalone mirror).
- [ ] Orphan `profiles.json` keys for removed models (harmless stale tuning; could prune).
- [ ] **(SECURITY) Tar-slip in `import_unitypackage.py extract_tree`** (`--tree` path only): `pathname` from
  the package is joined to `out_dir` with no `..`/absolute containment check. The UI import path uses
  `basename` and is safe; only a manual `--tree` on a malicious package is exposed. Add a containment assert.

## INTENT OVERHAUL -- audit #9 reconciliation (2026-06-26). FIXED + suite 186/0/11 + python 6/6 + node --check clean.
**48 verified audit findings drove a large intent-based overhaul; the full JS suite went 148 -> 186 pass (0 fail, 11 skip). All of the below are DONE + verified; the tests now assert INTENT, not the old behavior.**
- **CRITICAL: VRM bodies move.** `vrm.humanoid.autoUpdateHumanBones=false` at load so `vrm.update()` no longer wipes the AI pose every frame; `tests/vrm_order.test.js` proves it.
- **Compositor = TRUE sum-then-cap.** Same-role layers SUM, then the summed offset is clamped ONCE to the joint limit (was cap-per-layer-then-compose); the `flex` channel honors per-role joint limits too (was a flat +-2.2 rad).
- **Velocity-continuous smoothing BUILT.** Per-frame angular delta is clamped to `bone_limits.json` `speed_limit` (deg/s) -- the "never janky" layer; `speed_limit` is now ENFORCED, not just advertised. The layer `speed` param is now meaningful for data layers (was a no-op).
- **STRICT BLINK.** The free-running auto-blink timer is REMOVED; a blink fires ONLY on a real drive (speech onset via `onSpeakStart`, or the bus `blink` / a held `setBlink`). Both facades expose `blink()`/`setBlink()` with one shape; VRM gained `setBlink`/`blinkMode`/`ownedMorphs` and no longer sticks half-closed.
- **PLACEHOLDER RETIRED.** `default_avatar.js` returns an inert `NoModelMarker` (0 bones / 0 roles); the no-model hint is an ASCII DOM message.
- **Honest-failure fixes.** Bus `mouth`/`blink` are `isFinite`-guarded (no NaN-poisoning the jaw); `.obj`/`.dae` dropped from loadable formats (honest "unsupported", no fake JSON error); FBX material-bind failures surface to the log; `say.py` fails honestly on bad args with ASCII help; `perform`'s `[look:dir]` gained 4 diagonals + an explicit `look:px,py` point form and pushes an honest `look-skip:` marker; commanded `lookAt` no longer silently no-ops when cursor-follow is off.
- **`[pose:role=val]` drives a full [pitch,yaw,roll] triple** (control.js), consumed by avatar.js's perform dispatcher.
- **Dead code purged.** procedural.js `twoBoneIK` + clap anchors + `_cp` scratch; the no-op `poke` IPC chain (avatar.js + main.js + preload.js).
- **Rig.** A name-resolved quadruped no longer bypasses the geometry upright gate (no false arm/torso roles on creatures); minimal `Leg/Leg2` naming handled.
- **Spring.** Geometric-fallback chains no longer sag under gravity at rest (gravity gated on real body motion).
- **Physics.** Thrown props use rapier CCD (`enableCcd(true)`) + thicker platform slabs (no tunneling). `conjure.js` `moveTo` no longer yanks a 2D prop's depth to 0 when z is omitted.
- **UI.** The Jiggle/Cloth/Morphs range SLIDERS are now numerical inputs (the hard "NO sliders" rule); the attach-bone picker is capability/role-driven, not a hardcoded human-bone list.
- **Diagnostics realigned to the engine.** `rig_report.mjs`/`describe.mjs` run the `resolveBetween` (tier 3.5) step + carry the current facial regexes + a blink-channel probe; `audit_faces.py` mirrors facial.js v2's TWO independent mouth/blink ladders.
- **Tests assert INTENT.** The realmodels regression suite gained an `AVATAR_MODELS_DIR` override + a guard that fails loudly on an all-skip masquerade; weak settings/gallery/conjure tests hardened; the new `vrm_order` test.

## ⚡ LEAN PASS — no gestures, no per-model overrides, full per-finger control (2026-06-25)
**User: "make sure there are no gestures... nothing made specifically for any avatar... more lean", then "do it" (strip the override mechanism too).** Done + suite **152/0/11** (now **148/0/11** — the later line-by-line cleanup dropped 4 dead-math tests) + tools `node --check` clean.
- **Gestures/emotes/clips: PURGED** ("purge means purge") — the `gesture()`/`express` catalog, the `express` bus action + relay, and the orphaned `play`/`loop` actions are gone. Motion is composed ONLY from primitives: `pose`/`flex` layers + per-finger curl, authored by the AI via `perform`. The ball-physics shim was renamed `gesture()` -> `ball()`.
- **Per-model `rig_overrides.json` mechanism: REMOVED END-TO-END.** Deleted: the JSON file, the loader, `applyOverride` + the resolver's force/exclude tier (`rig.js`), the spring `extra`/`never` hook (`spring.js`), the facial `mouthMorph`/`blinkMorphs` index-override hooks (`facial.js`), the diagnostic tools' `loadOverrides`/`--no-override` (`tools/rig_report.mjs` + `describe.mjs`), and all 8 override-specific tests. Behavior is byte-identical (proven: `realmodels.test.js` locks the per-model role counts and still passes). The resolver is now purely generic (VRM -> name -> geometry -> between). The lone per-model eye-flip became the global default `EYE.flipY: -1`.
- **Full per-finger control: BUILT.** Finger joints group into named chains; `setFingers(side, spec)` drives any finger 0..1 over the reactive carry-grip; bus action `fingers`/`hand`; `capabilities().channels.fingers` lists them. Verified by `tests/handwave.test.js` (raise -> fist -> one finger up -> wag "no", asserting each phase on a synthetic 5-finger skeleton).
- **Docs reconciled:** root `README.md` rewritten to separate **Enigma (the LLM)** from **the Avatar overlay** and the external `C:\Users\SirKn\3d Avatar\` models/spec folder; `STATUS.md` gained a "CURRENT STATE" block; stale `rig_overrides` references swept from code comments + tool docs.
- **FOLLOW-UP — DONE 2026-06-25 (cleanup pass):** the stale README/STATUS sections were reconciled to the `pose`/`flex`/`perform`/`fingers` surface (test count reconciled — now 148/0/11 after the line-by-line cleanup dropped 4 dead-math tests; the purged `express`/`gripFingers` grammar swept), and `say.py` was confirmed ALREADY lean — its `express`/`play`/`loop` commands were gone and it now fails honestly on an unknown verb. Also removed the last dead clip-library leftover: the `avatar:listAnims` IPC handler (`main.js`) + its `preload.js` bridge + the empty `anims/` folder. STILL-OPEN (cosmetic): makiro's hair top-pin (a removed per-model spring nicety) may now slab — live-check.

## ⚡ AUDIT #8 — P1-P4 / f94b288d batch, trust-nothing pass (2026-06-25). FIXED + suite 163/0/11 + live-verified.
**Audited CLEAN (suspicions refuted by reading the code):** the f94b288d joint cap is correct — `clamp` converts the degree limit table to radians (`*DEG`) before clamping the radian offset (cap test: 2.0rad -> ~40deg); `perform [look:...]` -> `EnigmaAvatar.lookAt(px,py)` exists. **Real defects FOUND + FIXED in the conjure/control path:** (1) `perform`'s `[conjure:NAME]` and the `conjure` command passed a bare name straight to `loadAsset` as a URL -> a silent no-op (no name->asset resolver; the control.js example even uses `[conjure:sword]`). Added `resolvePropName` (control.js, unit-tested) + a `CONJURE_ASSETS` map -> a bare name maps to a known prop, a path passes through, an unknown name returns an HONEST error instead of silently spawning a guess. (2) conjure load FAILURES vanished silently while the bus reply already claimed success (violates "fail honestly") -> `conjure.js` now takes an `onMiss` cb (unit-tested); avatar.js surfaces it via setStatus + console.warn. (3) `perform`'s conjure branch never called `wake()` -> a conjure-only line never animated the pop-in; added wake + a transient `dur` so a performed prop self-dismisses (no accumulation across a conversation). (4) `capabilities()` now advertises `units {offsets:radians, limits:degrees}` (the brain read degree limits while sending radian offsets; the clamp made it SAFE but the intent was ambiguous). **DEFERRED item now RESOLVED 2026-06-26 (intent overhaul):** the flex channel now honors the per-role joint limits (was the flat +-2.2rad noted here), with the sum-then-cap rewrite. **FLAGGED (environment, not code):** `BALL_URL` (`props/worn_baseball_ball/...glb`) is MISSING from this checkout -> conjure/throwball/dropball have NO bundled asset to render here; stage the .glb (it lives in the standalone mirror; was the ~64MB physics-slice asset). Conjure props stay BRAIN-window-only on multi-monitor (same as physics props; deferred).
_Last updated: 2026-06-11. When an item is done, move it to the bottom “DONE” log so we never re-investigate it._

---

## A0. AUDIT FIX BATCH — LANDED 2026-06-10 (distrust audit, 3 agents + live battery)
All P1s from the 2026-06-10 audit are FIXED + test-pinned: NSFW breeze leak (BREEZE_SCALE was missing dick/anus after the region split → `?? 0.6` fallback gave them wind; now 0 + a test asserts every NSFW_REGIONS entry maps to 0); ambient idle wiggled NSFW LEAF bones (chain tips are childless → never sprung → fell through to ambient; buildAmbient now classifyBone-skips NSFW — Mal0 ambient 155→137 verified live); region false positives (TopKnot≠dick, Butterfly≠butt, Canal≠anus — lookarounds + tests; KnotBase still classifies); NaN-poisoning guards on every stringly bus entry point (size/setMorph/setMorphValue/springTune/facialTune/tune/wake — a garbage value could blank her UNRECOVERABLY or persist a frozen profile); spring NaN self-heal now resets tip/prev too (one NaN event used to dead-pin the bone forever); Parts section default-OPEN for real this time (`_partsOpen=false` init defeated the `!== false` check — the exact regression again); loadModel commits curKey only on the WINNING load (failed loads misrouted saves + made `query model` lie); gesture() during an airborne jump/flip now cancels the motion first (was: tumbling board doing a clap); getup-while-standing returns `{error}` instead of silent null; bus.py per-send 1s timeout (one stuck consumer head-of-line-blocked the hub); maybeIdleBehavior gated on motion/gesture (stale queued emote fired the moment she got up + fps ladder bounced during holds); ikTest restores the arm quats (idle-off probe froze arms at the clap centre); quit-reason logging in main.js (3rd unexplained clean exit — next one will say if it was hotkey/tray/window or external).
- [x] **Eye-look horizontal (yaw) — FIXED 2026-06-09.** Root cause: an eye bone's local-Y was its GAZE axis, so yawing about it just ROLLED the eyeball (invisible). `driveEyes` now rotates each eye about axes derived from its PARENT (face) frame — yaw about face-UP, pitch about ear-to-ear — converted into the bone's local space (`resolveEyes` precomputes `e.up`/`e.right`). Range raised (`EYE.gain` 0.8→1.15, maxX/Y up). VERIFIED numerically: gaze.x swings −0.55 (left) ↔ +0.35 (right); both eyes track together. Re-check any rig with bus `query eyegaze`.
- [ ] **makiro hair** — user: “fix her hair, only for her.” ⚠️ UNKNOWN what’s wrong (too floppy? clipping? wrong direction?). ASK, then bake a per-model spring profile into `profiles.json` (like c_za’s).
- [x] **Clap MEETS — 2-BONE IK SHIPPED 2026-06-09** (Task #45; user ruling: "just move the parts, not a huge list of animations" → IK-first, the §E2 clip-library idea is DEMOTED to optional). `twoBoneIK(armRole, forearmRole, handRole, worldTarget, pole)` in procedural.js — analytic (law of cosines + aim, orangeduck formulation), world-axes converted per-bone to local → works on any resolved rig. Clap v2 places each hand at a CHEST-LOCAL meeting anchor (pose/rotation-invariant; captured at build with armReach + body right/up): gap closes rest 1.61 → **0.14 wrist-to-wrist on every beat** (= palms touch), VERIFIED numerically (`query joints` handGap + `query iktest` per-arm residual = 0.000 both arms) AND visually (mid-beat snap: hands together center-front, chest height). The same solver now unlocks point / wave / reach / touch-object by just supplying a target.
- [x] **Multi-monitor — REWRITTEN 2026-06-10 (the "she vanishes between/across monitors" bug, killed at the root).** Abandoned moving ONE window across displays (unwinnable: Chromium never recomposites a transparent surface on a cross-display `setBounds` — electron#33103/#40515; the 2-step-hop + `forceRecomposite` were tricks, and `forceRecomposite` was already DEAD code). New architecture = **ONE stationary overlay window PER display + a single GLOBAL position (virtual-desktop DIP) owned by main.** Every window renders her at its own display offset, so she spans bezels / crosses monitors with zero repaint tricks. The PRIMARY display's window is the **brain** (runs animation + the Settings/menu UI + the AI bus); the others are **peers** that mirror the brain's per-frame pose broadcast (`Float32Array`: root quat/scale + motionY + every bone quat + morph influences → main → peers) and support grab. **Drag is main-owned**: any window reports the grab (offset in DIP), main then follows the OS cursor (`getCursorScreenPoint`, works across every monitor) until release → seamless cross-monitor drag. Click-through arbiter in main keeps exactly ONE window interactive (the cursor's). Bus connects on the brain ONLY (else N windows execute every command N×). Tray "Bring to monitor" sets the global pos. **VERIFIED LIVE (3-monitor rig): she renders on the primary (brain) AND on a secondary monitor (peer mirror, same live idle pose); position math correct on all 3 windows (centred where she is, GPU-clipped off-screen where she isn't = the spanning behaviour); single bus owner; suite 102/0.** The one real bug found+fixed in testing: `myBounds.w`→`.width` field mismatch (init payload is `{width,height}`) was rendering her ~1000× off-screen on every window — now locked by a `mathutil` unit test (DIP↔local-px round-trip + mixed-DPI + the field-name regression). Files: `main.js` (window-set manager), `avatar.js` (global-pos render + animator/peer split + pose serialize/apply + drag), `preload.js` (IPC), `mathutil.js` (`dipToLocalPx`/`localPxToDip`). ⚠️ **USER must still confirm on their actual screen: slow drag left→right across all 3 monitors stays visible, and parking her straddling a bezel shows both halves.** v1 LIMITATIONS (deferred): live material recolor/mesh-hide on the peer half updates only on reload (pose+morphs DO stream live); full right-click menu/Settings only on the primary window (drag works everywhere); spring Verlet velocity isn't transferred on the rare mid-drag animator handoff (no visible seam in practice). Old `placeOnDisplay`/`forceRecomposite`/drag-hop machinery deleted.
- [ ] **Reversed bones on some rigs?** — user mentioned bones “reversed or something”; verify per-rig (the head-look pitch was one such; check others).
- [x] **VRM models: `vrm.update()` STOMPS the procedural pose — FIXED 2026-06-26 (intent overhaul).** `vrm.humanoid.autoUpdateHumanBones=false` is set at load so `vrm.update()` no longer copies the rest-pose humanoid bones back over the AI pose every frame; the compositor now drives VRM bodies. Pinned by `tests/vrm_order.test.js`. (Was: latent, no VRM in the library — but it was a first-class rig tier producing ZERO visible motion on any `.vrm`.)
- [x] **Global hotkeys ≡ AltGr on non-US layouts — DONE** (verified 2026-06-26): every shortcut is now `CommandOrControl+Shift+Alt+...` (`main.js:574-585`); no `Ctrl+Alt+` family remains, so AltGr typing on EU layouts no longer triggers them.

## A2. MODEL VARIANTS / OUTFITS — how they work + how to activate (2026-06-09, user ask)
**Mal0's "different looks" are MESH TOGGLES, not morphs** (she has 0 shape keys). Parts list (already labeled): 9="body suit" (the jumpsuit), 10="skin" (the FULL detailed body underneath — hence her 47 NSFW jiggle bones: breast 8 / butt 6 / genital 33, weighted 0.5 in her profile). **ACTIVATE: Settings → Parts → uncheck "body suit"** (or bus/AI `setMesh {index:9,on:false}`; MCP avatar_command setMesh after the Desktop restart). Round-trip VERIFIED live; persists per model (profiles hiddenMeshes) across restarts. Same pattern applies to any model whose variants are separate meshes — check the Parts list first, then morphs (query meshes / morphs).
- [x] **"Outfits" preset UI — DONE** (verified 2026-06-26): named mesh-visibility presets per model — `outfitNames`/`saveOutfit`/`wearOutfit`/`deleteOutfit` (`avatar.js`), Settings UI (`ui.js`), bus `outfit` action, `query outfits`; persists per model.
- [ ] **Clothes jiggle ("clothes may need more weights"):** Mal0's suit has NO cloth bones (no cloth region) — it's skinned to the BODY bones, so it already follows the breast/butt spring weights (raise those and the suit moves too). INDEPENDENT cloth sway/sag needs the cloth/soft-body solver from the physics-engine item (§E) — same blocker as boneless-cloth. For models that DO have cloth bones, the existing cloth region slider covers it.

## B. NEEDS USER INPUT (ask, don’t guess)
- [ ] makiro hair — *what* exactly is wrong?
- [ ] “Some things that should show up for models still do not” — WHICH parts/morphs/toggles, WHICH models?
- [ ] “Did I miss something on Mal0?” — re-investigate Mal0 specifically.
- [ ] Eye direction (once horizontal works) + 3rd-monitor (fixed now?) — confirm.

## C. REQUESTED FEATURES — next builds
- [x] **2-bone IK solver — DONE 2026-06-09** (see §A clap entry). NEXT gestures are now cheap: point-at-screen-coord, wave, reach/touch — each is just a target + timing curve through `twoBoneIK`.
- [x] **Smooth jump / flip / lay-down** — DONE 2026-06-09: animated transform tweens (`motion()` in avatar.js — eased `pos.y` arc + `rig.rotation`, idle SUSPENDED via proc "hold"). Verified live: flip tumbles smoothly 0→180→360 and lands upright (no more snap). Bus: `gesture jump|flip|laydown|getup`. POLISH LATER: jump = add crouch/launch via leg-bone poses; lay-down = a relaxed lying pose (not a stiff side-tip); land squash.
- [ ] **Wander** — a proper behavior toggle (currently improvised via `moveTo`): stroll + pauses + glances.
- [ ] **Change clothes** — beyond mesh show/hide: outfit swap / layered clothing.
- [ ] **Folder rename** — rename the model FOLDER, not just the manifest label (changes the URL → migrate the profile).
- [x] **MCP `avatar_command` — EXPANDED 2026-06-09.** Now forwards gesture/express/rotate/lookAt/lookMode/goTo/regionWeight/morph/setMesh/monitor (widened the key-list + inputSchema + docs in `modkit_mcp.py`; `py_compile` clean). ⚠️ Takes effect ONLY after a Claude Desktop RESTART (the MCP server loads at startup). Until the user restarts, drive live via the raw bus (`tools/avbus.py`). This is the "AI controls her LIVE via official tools" enabler.

## D. VISION (the embodiment goal — bigger)
- [ ] **Sit on a chair** — spawn/anchor a chair prop + a sit pose (IK feet/hips) + position. Needs IK (C) + props + physics (E).
- [ ] **Throw a ball** — ball prop + throw motion (IK arm) + ball physics (trajectory/gravity/bounce). Needs IK + physics (E).
- [ ] **AI as an embodied agent** — drive her naturally as a body. Foundation in place: spatial awareness (screen-px in `avatar_status`, `goTo` anchors), gesture framework, idle-suspend.

## E. PHYSICS ENGINE ⚙️ (big architectural need — user flagged 2026-06-09)
- [ ] **Adopt a real physics engine** for the complex interactions. Today’s physics = spring-bone jiggle ONLY (no rigid bodies, collisions, prop gravity, cloth-vertex-sim).
  - Needs: rigid bodies (props/ball), collisions (avatar↔chair/floor/ball), constraints (sit/hold), soft-body/cloth.
  - Candidates to evaluate: **rapier.js** (Rust→WASM; fast, modern, deterministic — likely best), **cannon-es** (pure JS, simpler, slower), **ammo.js** (Bullet port; powerful but heavy/old). Constraint: no-admin install (npm, portable Node) — all three are npm/WASM, OK.
  - Integrate as a layer the avatar + props share; keep the existing spring-bone layer for hair/jiggle (cheap) and use the engine for rigid/cloth/interaction.
- [ ] **Boneless cloth** (e.g. Mal0’s cloth mesh has no bones → can’t bone-spring) → needs the physics engine or a dedicated cloth solver.

## E2. ANIMATION ROADMAP — research verdict (2026-06-09, cited in session)
> **⚠️ The CLIP items below (clip retargeting; bundling a CC0 clip pack) are SUPERSEDED by the 2026-06-25 lean pass** — the engine is now primitives-only (the AI composes `pose`/`flex` layers + per-finger control), and `retarget.js` was built and then REMOVED in the purge. Do NOT re-adopt authored clips without an explicit decision to reverse the lean pass. (The 2-bone IK item is DONE; the VRM fast-paths / normalized-proxy-skeleton items remain valid.)
Industry practice for arbitrary-rig full-body motion = **authored clips retargeted onto a normalized humanoid rig**; procedural is the recognized layer for REACTIVE motion (gaze/blink/idle/springs) and simple gestures. Our resolver+derived-axes system is "a from-scratch reinvention of the normalized-humanoid-rig idea — the right invention for unverified rigs" and is the hard part of retargeting ALREADY BUILT. Adopt next, in order:
- [ ] **Clip retargeting for gross motion** (better jump/flip/dance/sit): feed OUR role map into three.js `SkeletonUtils.retargetClip` (`getBoneName` + `localOffsets` rest-pose correction), or retarget via the pixiv `loadMixamoAnimation` pattern (rest-pose compensation: `parentRestWorldRot * trackQuat * restRotInv`).
- [ ] **Bundle Quaternius Universal Animation Library (CC0, 120+ humanoid clips incl. jump/sit/crawl/emotes)** as the shipped clip set — CC0 = ship-safe; allow user-side Mixamo import (Amica's model) but do NOT bundle Mixamo raw files (Adobe ToS forbids raw-file redistribution).
- [ ] **2-bone IK (Task #45): hand-roll the analytic orangeduck "Simple Two Joint IK"** (law-of-cosines + pole vector; pole = our derived forward axis). Avoid THREE.IK (unmaintained FABRIK); CCDIKSolver = maintained fallback; mine sketchpunklabs/ossos for IK-Rig design.
- [ ] **VRM fast paths when the model IS a VRM**: `vrm.lookAt` (respects author gaze limits) instead of our derived-axis eyes; `VRMHumanoid.getNormalizedBoneNode`; VRMA clips via `@pixiv/three-vrm-animation`.
- [ ] (Optional architecture upgrade) materialize a **load-time normalized proxy skeleton** (T-pose, world-aligned axes) from the resolver output and pose THAT (three-vrm normalized-bones model) — replaces per-motion axis derivation and doubles as the retarget target.

## F. TECH DEBT / TEST GAPS / MINOR
- [ ] **Triage residue from the archived audit blocks** (now under DONE / HISTORY LOG below): e.g. the audit-#3 P3 list — spring.js verlet not dt-/scale-normalized, no speech bubble, hueShift name-only — still-relevant tech debt that relocated with its block on 2026-06-25; pull anything you act on back up here.
- [ ] Multi-mesh morph — only the bounded “primary group” version is done; a model with *divergent per-mesh* morph layouts would need a per-group UI. (Low.)
- [ ] Morph/facial mouth collision — a saved morph on the exact lip-sync mouth index is overwritten each frame (now just labeled “auto · lip-sync”). (Low.)
- [ ] Test coverage — gesture system, eye-look, monitor recomposite, adaptive-fps `active` set are live-verified but NOT unit-tested. Extract testable logic where feasible (like mathutil.js / region.js).
- [ ] **AUDIT 2026-06-09 (trust-nothing) — still-open findings:** morphMeshes picks the primary group by morph COUNT not "is the face" (MED, no current model hits it); morph `auto` flag intersects `facial.ownedMorphs` (all-mesh indices) with the primary-group index space → can mislabel (MED, edge); `eyeSide` regex false-positives e.g. "center_eye"→R (LOW, correct on shipped rigs); head-look `-_lookY` bakes makiro's pitch convention → needs `LOOK.flipY` override on an opposite rig (LOW); Parts rename `onchange` loses an unblurred edit on panel switch (LOW).
- [~] **WEAK / MISSING tests (audit 2026-06-09) — partially CLOSED 2026-06-26 (intent overhaul):** realmodels.test.js no longer masquerades-by-skip (it gained an `AVATAR_MODELS_DIR` override + a guard that fails loudly on an all-skip), and the weak settings/gallery/conjure tests were hardened. STILL OPEN: the draggable-panel test passes regardless of the drag math (jsdom rect always 0); `dom.js` mock OMITS `importProp`/`importDropped`/`setInteractive` (prop-attach + drag-drop + click-through untestable); NO test for the click-through guard (`containsEvent`) or the delete-CURRENT-model branch. (Pure-logic suites region/mathutil/library/facial/geom_mouth/rig/compositor ARE solid — values hand-verified.)
- [ ] “Sleep when idle” deeper power optimization (offered) — freeze render when fully idle → ~another 35% off (leaves only the GPU-compositor floor). Off by default; make it a toggle.
- [ ] **AUDIT 2026-06-09 #2 (trust-nothing, 3 agents + live battery) — still-open LOW residue:** drag-rotate (rotateMode) while LYING shows a transient upright snap (pointerup commits correctly — visual only); `query joints` can return NaN on world-coincident role bones (diagnostic-only); motionmath dip MAGNITUDE coefficients unpinned by tests (sign/order/apex ARE pinned); knee-splay `abd` isn't FSIGN-corrected (could read knock-kneed on a mirrored rig — cosmetic 0.2rad); at FPS_REST the dt clamp runs animation time ~75% of wall (invisible). FIXED in this audit: lying-state cluster (motion/gesture from a lying hold now stands her cleanly first; getup-while-standing refused; interrupted flip/getup restores rotation), jump-interrupt pos.y strand (caught LIVE on glados), `_lookW` pinning 60fps through a whole lying hold, negative-dur soft-lock, unknown-gesture silent freeze (now errors), `query rotation` now returns LIVE rotation + lying/inMotion (was saved-only — misled the driver), recomposite stale-timer race (single slot + authoritative bounds), MCP `monitor value:"next"` schema type bug, axis derivation under a stale previous-model rotation (applyRotation now precedes buildProceduralRig), flex() ±2.2rad typo safety net, Move-submenu now actually TESTED (mock + click test), old hotkeys got the .catch guard.
- [ ] Orphan `profiles.json` keys (toothless / c_za / fexa_blender_2 / …) — harmless stale tuning for removed models; could prune.

---

## ⚠️ KEY LESSONS (read before touching motions / rig posing — saves hours)
- **A limb's local "pitch" is NOT flexion.** On makiro the upper-arm's local-X is ABDUCTION (spreads sideways), the forearm's is the elbow, legs vary again. Posing limbs on a fixed local axis is WRONG per-rig. Use `flex(role, ang)` in procedural.js — it rotates each limb about the BODY's left→right hinge axis (derived per-bone at build, like the eye fix), so forward/back flexion is correct on ANY rig and auto-mirrors L/R. TORSO/neck/head DO flex on local pitch (proven by emote signs) — leave those on `pose()`.
- **Front-view FORESHORTENING hides sagittal motion.** A forward crouch/tuck collapses to a near-straight SILHOUETTE head-on (you'll swear the legs aren't bending — they ARE; verify with bus `query joints` → knee 177°→~110° during a jump, or rotate her 90° to see the profile). The user sees her FRONT-ON, so motions need cues that read head-on: the jump now has a vertical SQUASH-AND-STRETCH dip (sink→spring→absorb) + hair physics, not just leg bend. Apply the same thinking to future motions.
- **DIAGNOSTIC bus queries (use these — they'd have saved this whole session):** `query roles` (role→actual bone name + flex axes), `query joints` (live knee/elbow angle, 180=straight + whether the embedded clip is overriding), `query eyegaze` (each eye's world gaze vector — x flips L↔R only if horizontal eye-look works). All in avatar.js `answerQuery`.

## DONE / HISTORY LOG (resolved -- do not re-investigate)

_The verbose audit/feature work-logs below were relocated here 2026-06-25 to keep the live backlog readable at the top. All content preserved verbatim, newest first._

## ⚡ AUDIT #7 — "the walls push her up when resized" ROOT-CAUSED: the MEASUREMENT pipeline (2026-06-12)
**The user's symptom = a POISONED bounds measurement, not the walls.** The load-time re-anchor trusted `SkinnedMesh.computeBoundingBox×matrixWorld` — which is computed against the BIND-time frame and returns garbage after we scale/shift/normalize (her body mesh measured 0.26 units, one hair object 7.4; dims hit 12.8–34 on a 3.5-unit model) → the anchor shift floated the MESH high above her base ("pushed up"), the shadow exploded, and fitToScreen crushed her size. **FIX (layered):** ① measure BONES, not mesh — deforming-bone world positions (skin-weight map), mass-weighted 1–99% percentile bounds (a parked-but-weighted IK helper can't define the box), HEIGHT from the feet→head ROLE span + headroom on resolved humanoids (anatomy beats statistics — hair bones are heavy AND long); statics (statues, no bones) keep honest rigid-geometry boxes; ② measurement moved AFTER the gravity counter-rotation (it was catching the pre-fix hair plume), springs rebuilt after the shift; ③ SANITY GATE backstop: any measurement past 2.5×BASE_H is rejected loudly (`POISONED bounds` in the main log), bind dims kept, anchor untouched. **VERIFIED: she renders mesh-on-base with a proper shadow; size recovers; resize is position-neutral in every state (floor/platform/sitting — probed over the bus, pos pinned).** Also this round: **floor moved to the WORK-AREA bottom** (taskbar top — the display bottom is a 48px strip no window can draw; the overlay window itself is work-area-clamped by Windows), the physics floor follows it; refreshDims skips mid-gesture (audit P2). **Precision leftover:** dims.h reads ~7.3 vs the true ~4.9 on the catgirl (role-span override needs a second look — affects only the auto-fit margin, nothing visible); peer-window render on the REARRANGED monitor layout needs eyes (one stale-looking peer capture observed mid-investigation — may have been the dims poison; re-check after this fix).

## ⚡ FLOOR = SNAP LINE (not a wall) + AI PLATFORMS + PICK/GRAB FIX + REAL-BONES LIST (2026-06-12, user feedback round)
- **GRAB WAS BEING EATEN BY A STUCK BONE-PICK** ("i do not know what you did but now when i grab…"): arming Pick then clicking empty space left it armed forever (and it survived model switches) — the next click on her picked a bone instead of dragging. Pick is now strictly ONE-SHOT: a click on her picks, a click anywhere else cancels, model switches clear it, Esc cancels. Emoji removed from the button (user ask).
- **WALLS v2 — fixed + permeable** ("when resizing the avatars wall is moved as well… i need the avatar to act like there is floor there whial being able to go through it"): the body-scaled walls were WRONG (they moved with resize) → slim FIXED margins (16px glide / 8px drag backstop). The BOTTOM is now PERMEABLE: **the floor is a SNAP LINE** — released (or glide-arriving) within ~5% of the screen bottom or of a PLATFORM top, her feet ease onto that surface; nothing hard-blocks pushing her past it (clamps allow 1.4× screen height below; tray/goTo recovers). **LIVE-VERIFIED: moveTo 40px above a platform → settled exactly ON it (py 1000); moveTo 48px above the screen bottom → settled ON the floor line (1438).**
- **AI PLATFORMS** ("i would rather the AI be able to place 'platforms' to make effects"): bus `platform {px,py,w}` adds an effect surface (screen-px on her monitor), `{clear:true}` wipes; `query platforms`. Each is a translucent bar rendered in EVERY window + a rapier static slab in the brain (balls roll on it — same collider pattern as the verified floor) + a snap target for her feet. YES it works (asked "would this even work?") — statics are native to rapier; her side is release-snap. Backlog: platforms don't persist across restarts (ephemeral effects by design), late-created windows miss existing bars until re-set.
- **REAL-BONES LIST** ("from seeing the bones it seems our bone system is bad"): the SYSTEM is sound — the soup is the files' (593 Rigify helpers). The Bones panel now defaults to the bones that MATTER (deforming ≥0.5 weight mass / role-resolved / already named — catgirl: ~170 of 593), sorted roles-first then by real mesh influence, with a "show N helper bones" checkbox (auto-ticked when a pick lands on a helper). bones() now reports mass+deforms per bone (AI-visible too).

## ⚡ FULL-SYSTEM BATTERY (2026-06-12, user: "lets test everything") — `tools/test_battery.py` is the regression sweep; RUN IT after engine changes
**RESULT: 15/15 models load; every humanoid resolves 19/19 roles; behaviors all pass** (gestures cycle to clean neutral, emotes, highlight/nameBone, outfit wear/persist, recolor, mesh toggle, impulse, physics ball, body-aware walls verified bottom-flush + 8px sides, monitor hop with peer render, stillness BYTE-IDENTICAL once settled). **Three real bugs found + fixed by the sweep:** ① fully-folded binds (aveline 8.8°/20.4° knees) are PACKAGING poses, not squats — leg normalization now covers ~35–150° only (test-pinned); ② modelDims LIED three ways — counted hidden meshes, used UNSKINNED geometry bounds (parts authored side-by-side + assembled by skinning), and let paper-flat ground/backdrop sheets shipped INSIDE avatar files define her size (aveline: a 34-unit-wide zero-height plane → crushed scale + a "giant shadow" that was actually her own backdrop rendering) — bounds are now visible-only + skinning-aware + flat-sheet-excluded, refreshed on part/outfit toggles (`query bounds` diagnostic); ③ mesh-INDEX stability — hiddenMeshes/labels/colors are index-keyed but bone reparenting (surgery/adoption) shifts traversal — the mesh list is now CACHED in pristine file order at load. Aveline's 6th backdrop plane hidden + persisted (she's a hover-fold robot by design). **Notes:** battery "stillness" must wait ~10s after a model switch (spring settle reads as DIFFER — benign); spring COUNTS vary a few bones between identical loads (coincidence-tolerance boundary flips in the twin dedup — behavior unaffected; tighten if it ever matters). Known per-model truths, not bugs: marie = 0 roles (names destroyed — needs the manual mapping session), bunny-robot = statue (no joints).

## ⚡ SYSTEM-WORK SWEEP (2026-06-12, user: "lets do all system work now") — the engine layer for RANDOM models
**All committed + live-verified; suite 138/129/0.** ① **SKIN-WEIGHT PASS** (`skinweights.js`, unit-tested): per-bone vertex weight mass at every load; any DEFORMING bone coincident with a role bone but outside its subtree is AUTO-ADOPTED under it (the parallel-limb surgery, generalized — no more hand-written maps); coincident SPRUNG chain roots dedup (highest-mass chain springs, twins ride it). Live on the catgirl: 170/593 bones deform, +6 twins beyond the manual map, 4 chain groups → 73 bones un-sprung (the triple tail + double ears, fixed generically). `query weights`. ② **CLIP RETARGETING** (`retarget.js`, agent-authored, algebra unit-tested): Mixamo-convention clips → ANY resolved rig via the role map + rest-pose compensation (A·q·B per bone, identity-exact; hips height-scaled). `anims/` library folder (drop GLB/FBX clips in), bus `anim {name|url, loop, stop}`, `playAnim/stopAnim`. ⚠️ no clips BUNDLED yet (CC0 pack download pending) — drop a Mixamo file in anims/ to use it. ③ **FACIAL v2** (per-channel): mouth + blink resolve INDEPENDENT ladders (override → named morph [widened: ARKit/UE/VRoid/CC/MMD] → bones [lids no longer gated behind a jaw] → GEOMETRIC `geom_face.js` head-anchored eye/mouth bands with mirrored-twin detection); `face.blinkMorphs` override added; `blinkMode` reported. **LIVE: glados BLINKS (2 flap-lids, no jaw)**; catgirl honestly none. ④ **SIT v1** (floor sit): legs fold under + root sinks 30%, HELD until getup (snap-verified); `gesture sit`. ⑤ **OUTFIT PRESETS**: named hidden-mesh looks per model — Settings → Parts bar (save/wear/delete), bus `outfit`, `query outfits`; round-trip persisted live. ⑥ **HOUSEKEEPING**: hotkeys → Ctrl+SHIFT+Alt (plain Ctrl+Alt is AltGr on EU layouts — typing "@" on QWERTZ used to QUIT her); **Electron 30 → 42.4.0** (boots clean: transparency/multi-window/bus/stance/gestures all verified on the new runtime); FBX import was already in (loader.js); renderer LOAD failures now forward to the main log (found chasing a "broken" load that was actually a model deleted from disk — shibahu's folder is GONE, models/_trash may hold it). **Deliberately deferred:** cloth/vertex sim + sit-on-chair constraints (needs its own session), cross-model skinned clothing transfer (research-grade; outfits + attachments cover today), physics side walls (balls exit by design), CC0 clip-pack bundling (download/license pass).

## ⚡ BONE IDENTIFICATION + SHADOW-AT-FEET + BODY-AWARE WALLS (2026-06-12, user: "a way of identifying them… the shadow… the walls… more room")
- **IDENTIFY bones two ways:** ① Settings → Bones rows — HOVER/CLICK a raw bone name → a hot-pink marker rides that bone on her body for a moment (relayed: shows on whichever monitor she's on; `highlightBone` is also a bus action so the AI can point at a part while talking about it). ② **🎯 Pick mode** (button above the filter): the next click ON HER selects the nearest bone (screen-space projection of every joint) → its name lands in the filter + lights up; Esc cancels. The see-it→click-it→name-it loop. jsdom-tested (hover→highlightBone; pick→filter). Other identification ideas if these don't suffice: wiggle-the-bone-briefly, full-skeleton view with click-to-select on the overlay ("B" shows bones already), per-chain colorization.
- **SHADOW WAS AT HER SHINS — root cause: feet anchor + modelDims measured from the BIND box.** Standing a squat-bound rig up extends the mesh BELOW the box measured at load → the foot-anchor line (= shadow position, grab footprint, physics-capsule height) sat mid-leg ("her bottom hit box is at her legs"). FIX: re-anchor + re-measure AFTER buildProceduralRig (rotation neutralized for the measurement; before the spring build so verlet tips capture from corrected positions; fitToScreen re-run since the true height can exceed the bind height). **Live-verified: snap shows the contact shadow under her boots.** Benefits every squat/slouch-bound rip automatically.
- **WALLS ARE BODY-AWARE NOW ("connected to the avatar… more room"):** the glide/nudge clamp scales with HER on-screen body instead of a flat 4% screen margin — feet can stand FLUSH on the bottom edge (was hovering ~55px above the taskbar — half the weird-shadow read), the sides let her lean past the edge as long as a grabbable slice (≥24px / 40% of her half-width) stays on-screen, the top keeps ~35% of her visible. The drag backstop in main loosened to 8px sides / flush bottom. **Live-verified: moveTo bottom → posScreen y=1438/1440.** (Physics walls: there are none — only the floor slab; balls fly off the sides by design. Add side walls to physics if contained-bouncing is ever wanted.)

## ⚡ THE IDLE ANIMATION IS DELETED — EVERYWHERE (2026-06-12, user: "delete the idle animation everywhere and anything that has to do with it")
**Not zeroed defaults — the MACHINERY is gone from the codebase.** Deleted outright: procedural.js idle layers (breath/sigh, postural sway, weight-shift stance machine + damped-spring ladder, arm hang/pose scheduler + IK anchors, glances, wrist noise, ambient micro-motion on unresolved bones, fidget scheduler), the whole `params`/`tune` surface (`setParams`, bus `tune`, `EnigmaAvatar.tune`), idleprofile.js + its seeder + both idle test files, the Settings "Idle" section + sliders + re-seed button + the "Idle motion" checkbox + the `idleOn` flag (proc.update is purely reactive/commanded and always runs), `query idle`/idleState (a slim `query grip` remains for the reactive grip), the spring BREEZE (param, BREEZE_SCALE table, per-frame wind force, the Settings knob, profile keys), profiles.json idle/breeze blocks, mathutil `ambientAmp`, motionmath `dampSpring/noise1/fnoise/jitter` (bell/easeInOut/jumpElevation stay — gesture math). **What still moves her (all reactive/commanded, per the standing ruling): cursor-look, blink (Settings "Face"), springs reacting to real movement, commanded gestures/emotes/speech/impulses, finger GRIP while carried, physics props.** update() now: gesture branch → emote offsets → static base pose (rest + look + emote) → gesture-exit blend → grip. **Suite-pinned structurally: "BIT-STILL by default" (1200 frames, every bone quat identical to 1e-12) + "springs DEAD STILL at rest"; LIVE-PROVEN: two renderer captures 8s apart are BYTE-IDENTICAL (SHA256), clap still fires + releases clean (elbow 180→44.9→180), grip diagnostic live.** Suite 123/114/0. ⚠️ NEVER re-add self-generated motion — not as a default, not as a tunable, not as a seed. If she's ever to move on her own again, the user asks for it per avatar, explicitly.
- **Noticed-fixes ridden along (same order):** ① `query iktest` now passes the same elbow pole the clap uses — a perfectly straight bind (180° elbow, her left arm) reported "solve-failed" with pole=null while real gestures worked. ② **Alt+drag SPIN now registers a spin HOLD with main** (dragStart {spin:true}): same single-owner arbiter freeze + heartbeat watchdog as a move-drag (its capture survives bezels too; previously stalled mid-spin at a bezel), but main does NOT follow the cursor and the pose-stream `drag` flag stays false (a spin is not a carry — no finger grip, no glide suppression). ③ catgirl triple-sprung tail/ears (ORG+control+DEF chains all springing): deliberately DEFERRED — excluding the non-DEF twins without weight data risks deadening the tail (the limb lesson in reverse); needs a weight-informed per-model decision.

## ⚡ AUDIT #6 — trust-nothing on the c36a817 batch (3 skeptical agents + live battery; user: "do an audit on the code and do not trust anything")
**Surviving findings, ALL FIXED + verified (suite 139/130/0; live restart):** ① **heel regression in the new foot re-aim** — it always FLATTENED the toes; the old code kept authored pitch unless extreme (>27°). Now the bind toe's MODEST pitch (≤0.45 |y|) is preserved through the heading fix (heels keep their tilt; true squat ankle-pitch still levels) — catgirl re-verified: stance numerically/visually unchanged (her small authored toe pitch now correctly kept). ② **surgery skips were INVISIBLE** — renderer console.warn reaches no file; a typo'd override name (or a re-exported model changing names) would silently strand limbs again. All surgery warns now forward to the MAIN log + a per-load summary line; **live proof: `surgery: 16 move(s) applied, 0 skipped` from ALL THREE windows** (also empirically confirms brain/peer surgery symmetry → pose-buffer bone order stays in sync). ③ latest-grab-wins on a mid-drag dragStart documented as DELIBERATE (an agent proposed gating on `held` — REJECTED: after a capture-loss drop `held` lingers until the next pointerup, so the gate would make her ungrabbable).
**Auditor claims REFUTED (recorded so they're not re-chased):** "P1: rig_overrides names lack dots / three.js doesn't sanitize / the surgery never ran" — FALSE: live `query roles` returns dot-stripped names, the head surgery with identical naming has worked since yesterday, the legs visibly changed with only the map added, ambient 452→262, and the new proof line shows 16/16 applied. "kneeW can normalize a near-zero vector (NaN)" — impossible: the knee gate (<150°) bounds |û+ŵ| ≥ 0.52 mathematically. Watchdog false-positive scenarios — traced safe (needs BOTH >48px AND >1.2s beat silence; a motionless hold never trips). **Latent (logged, accepted):** Alt+drag SPIN is renderer-local with no arbiter freeze → crossing a bezel mid-spin can stall the spin (self-heals via the buttons check; pre-existing, cosmetic); a peer's open Settings panel goes click-through during a drag on another monitor (by design).

## ⚡ DRAG v2 (single-owner capture) + THE BONE ISSUE ROOT-CAUSED (parallel DEF LIMB chains) — 2026-06-11, user: "rewright the code i do not care just make it work same with the bone issue"
- **DRAG REWRITTEN as single-owner capture.** The previous design handed window interactivity to the cursor's display MID-DRAG — flipping the captured grab window to click-through is what KILLED its OS mouse capture, so the release reached no window / the wrong window, and the display-id filter patch raced at the seam (both "stuck at the bezel" and "glued to the cursor" came from this one flaw). New invariant: **the arbiter FREEZES on the grab window for the whole drag** (Win32 capture is system-wide → it receives pointermove/pointerup on EVERY monitor); dragEnd is **reason-coded** ("up" = real release, honored from ANY window; "cancel" = pointercancel/blur safety nets, honored ONLY from the grab window); a **dead-man watchdog** in main drops her in place if the cursor keeps moving >48px while the grab window's pointermove heartbeats (dragBeat, ~25Hz) have been silent >1.2s (capture lost to Win+L/UAC with the cancel swallowed — "stuck to cursor forever" is now impossible by construction; a motionless hold never trips it). Tray "Drop her" stays as the manual hatch. ⚠️ USER-EYES: slow-drag her across all monitors + release on each — code-verified + relaunched, but physical input can't be self-tested (the dev-run overlay isn't grantable to the computer-use bridge).
- **THE BONE ISSUE = Rigify PARALLEL **LIMB** CHAINS (the hair/neck disease, fourth occurrence).** anime_catgirl's mesh-deforming DEF-thigh/DEF-upper_arm chains root on ORG-spine/ORG-shoulder as SIBLINGS of the ORG limbs the roles drive (follow-constraints baked away by the export) — standing the ORG legs up rendered a BLEND of standing (ORG-weighted verts) + bind-squat (DEF-weighted verts) = the scissored knock-knee smear; arm gestures smeared the same way, and the FINGERS hang under DEF-hand so curls never showed on her. **FIX: rig_overrides `reparent` — a new per-NODE surgery op (sibling of reparentChildrenOf, same attach()-preserves-world + cycle guard)** — each DEF limb segment moved under its ORG twin (8 leg + 6 arm moves; the .001 bendy halves + fingers ride as children). **VERIFIED LIVE: stance clean (legs parallel, hip→knee→foot all at x=±0.337, knees 180°, kneecaps 0–1.3° off the toes), full-body snap shows straight uncrossed legs; clap snap shows both visible arms up + bent (left elbow 48°, hand gap 0.258 closing) — pre-surgery that gesture smeared.** Ambient 452→262 (DEF limb bones now classify under limbs and drop out of ambient — bonus on the helper-soup item).
- **Engine-level knee-facing fix (general, test-pinned):** the straight-down leg aim is a MINIMAL rotation — it parked the leg's twist wherever the bind's fold plane was (kneecaps rolled inward = knock-knee read; feet inheriting the rolled heading = pigeon-toes; also closes the logged "antiparallel thigh arbitrary facing" latent). Now: kneecap direction −(û+ŵ) + toe heading captured at BIND → after standing the leg up, the thigh is TWISTED about rig-up so the kneecap faces the bind's own toe heading, and the foot is re-aimed to that same heading (was "keep whatever the aims left", gate 27° pitch → now 6° off-bind). On the catgirl the needed twist was tiny (0° / −3.6° — her visible smear was the parallel chains above); the rolled-hinge squat fixture proves the big-roll case (new test: kneecap tracks toes <10°, toes keep the bind heading <10°). Suite 139/130/0.
- **New diagnostics/actions:** bus `query stance` (knee°, hip/knee/foot world positions, toe headings, bindKnee/twistDeg/kneecapOffToes/toesOffBind on squat-normalized rigs), bus `nameBone {bone,label}` (the AI's handle for naming bones — same relay as the Settings input). **Bone naming PROVEN end-to-end live:** bus nameBone → relay → profiles.json on disk (ORG-thighL_031 = "left thigh" persisted — kept, it's a correct label). `tools/dump_bone_tree.py` = the GLB node-graph + skin-joint dumper that found the disease (regex arg; run it on any model that smears).
- **Latent (logged, not fixed):** `query iktest` reports left-arm "solve-failed" on the catgirl — her left elbow binds at EXACTLY 180° and ikTest passes pole=null (degenerate bend plane); REAL gestures pass a pole and solve fine (clap verified live). Fix someday: ikTest should pass the same default pole the gestures use.

## ⚡ PER-MODEL IDLE PIVOT (2026-06-11) — **SUPERSEDED 2026-06-12: the idle machinery is DELETED ENTIRELY (see the section above); the per-model opt-in surface described below no longer exists**
- **THE IDLE IS GONE, FULLY.** procedural.js ships every idle layer at ZERO (engine = toolbox, no personality) and **nothing is auto-seeded — every avatar stands perfectly STILL by default** (first auto-seeded version overstepped; the seeded idle blocks were stripped from profiles.json same day). A model gets behaviors ONLY when the user gives them to HER: **Settings → "Idle — this avatar's own behavior"** (sliders for every layer + the explicit "Re-seed from this model's capabilities" button = the opt-in starting point via `idleprofile.js seedIdleProfile`), or bus `tune` — persisted per model in profiles.json `idle`. Stray always-on motion gated too: arm-hang/finger noise behind `armLife`, glance pitch rides `look` (was a bare 0.05), scheduler zero-guards (`shiftEvery/poseEvery/fidgetEvery: 0 = never`, held arm-pose releases cleanly if tuned off mid-hold). Reactive channels (cursor-look, blink, gestures, emotes, lip-sync, springs, grip) are NOT idle and stay live. Suite-pinned: engine-default = bit-still after settle (<0.006° over 5s), seeds differ by capability class, NSFW never fidget-seeded, Settings sliders round-trip display scales. **The workflow: pick a model, say what SHE should do, we add exactly that to her file — model by model, nothing automatic.**
- **ROTATE-BY-DRAG MODE REMOVED → Alt+drag.** "i cannot move the avatar but i can rotate" (2026-06-11) = the Settings rotate-mode checkbox was armed, hijacking the primary drag gesture into spin. A modal toggle that eats plain-drag is a UX trap → plain drag ALWAYS moves; **hold Alt + drag = rotate** (hint in Settings replaces the checkbox; `rotateMode` stays as a bus/AI action only and is now visible in `query state` toggles for diagnosis; Settings-close still force-disarms it).
- **DESKTOP SHORTCUT FIXED — launcher runs Electron DIRECTLY.** Root cause: Start-Avatar.ps1 hard-required `%LOCALAPPDATA%\node-portable`, which agent-side setup installed INSIDE Claude Desktop's MSIX sandbox LocalCache (`Claude_pzs8sxrjxfjjc\LocalCache\Local\node-portable`) — invisible to the real desktop session → double-click died at the Node check, silently (hidden window). New launcher: `node_modules\electron\dist\electron.exe .` directly (repo paths are REAL for both sides; node/npm only for the one-time install, searched portable→repo tools→PATH), failures pop a VISIBLE WScript dialog, EAP relaxed around the electron pipe so PS 5.1's NativeCommandError-on-stderr can't kill the tee (also why the old log was empty — npm.cmd buffered output, killed before flush).

## ⚡ NO-IDLE SWEEP 3 — THE RANDOM-EMOTE SCHEDULER (the actual "it"; user: "idle was not removed. remove all of it") + DRAG DIES AT THE BEZEL (2026-06-11)
- **`maybeIdleBehavior` REMOVED OUTRIGHT** — the random-emote scheduler (nod/happy/alert/wag every 8–20s when left alone) predated the pivot, defaulted ON, and survived all three idle purges: it WAS the "idle still there". Deleted: scheduler, timer state, IDLE_EMOTES, the idleBehaviorOn flag, the Settings checkbox, the state-toggle key. **NOTHING self-fires now.** Emotes happen only when commanded (tap/pet, menu, bus, AI). Complete inventory of what may move her: cursor head/eye-tracking (reactive), blink (facial toggle — kept deliberately, kill via Settings "Face" if unwanted), springs reacting to real movement, commanded gestures/emotes/speech, physics props.
- **Stuck BETWEEN monitors (drag) — display-id filter (SUPERSEDED same day):** the filter treated the symptom and was racy at the seam (and could discard the REAL release when the old window still held capture). Replaced by DRAG v2 single-owner capture, see the section above.

## ⚡ NO-IDLE SWEEP 2 + ALWAYS-ON-TOP (2026-06-11; user: "it is still there" + "why is it not showing as the top window at all times")
- **The last self-generated motion sources are OFF:** spring **breeze defaults 0** (ambient wind swayed every hair/tail/whisker chain forever — that read as "the idle is still there"; springs still REACT to real movement; per-model opt-in via the spring profile / Settings breeze slider; my old saved breezes on makiro/catgirl zeroed) and the **idle eye DARTS are removed** (eyes track the cursor, return to center otherwise — reactive only). Still alive by design: cursor-look, blink, gesture/emote responses, physics reactions.
- **Always-on-top hardened:** windows now use the **"screen-saver"** level (was "floating" — sits BELOW other topmost apps) + a 4s **re-assert tick** (`setAlwaysOnTop` if demoted + `moveTop()`, never focuses) because Windows orders topmost windows by recency — anything topmost created after her used to cover her. If some app STILL covers her it's also screen-saver-level and newer — the tick reclaims within 4s.

## ⚡ AUDIT #5 — trust-nothing on the 2026-06-11 batch (2 skeptical agents + live battery; "do an audit. trust nothing and test")
**ALL FIXED + suite-pinned (138 tests / 129 pass / 0 fail).** Found: **P1 — the saved per-model rotation POISONED bind normalization** (applyRotation precedes buildProceduralRig; a saved pitch 12–72° — one Alt-drag away — made an upright model read "slouch-bound" and fold at the hips on every reload; FIX: the whole normalization works in a RIG-LOCAL frame now — gates/targets conjugated by the model parent's quaternion, so the user rotation cancels; FSIGN toe-probe deliberately stays world). **P2s (all empirically confirmed by the agent, all fixed):** poseEvery 0→N permanently bricked arm poses (armNext=Infinity never re-scheduled → re-arm on re-enable); zeroing shiftEvery mid-stance froze her in ±7° contrapposto forever (wPose now released → springs ease to neutral); the chest/neck/head un-slouch was DEAD CODE (hips frac 1 verticalized the position line → loop never ran; hips now 0.65 → distribution executes — catgirl fwd tilt −0.25→−0.09 live); arms were aimed BEFORE the trunk leveled (un-aimed ~15° on her; order now trunk→arms→legs, which also fixes restAdjust composition for under-arm chains); lying-bind class bug (knee-gate is orientation-blind → a lying rig's legs would fold 90° off-body; whole normalization now gated on bind-upright — a lying bind is a STYLE). Renderer-side fixes: counter-rotation no longer applies to ARM aims (sleeve/ribbon regression + composition risk), surgery cycle guard (a bad override could hang the renderer), bus `tune` sanitized via idleTune (stringly NaN-poisoning) + reqId stripped (was persisting into prof.idle), fidgetRegions array survives idleTune (numericOnly coerced []→0), launcher pops a dialog on non-zero electron exit, shiftEvery/fidgetEvery floored at 2s (slider's bottom notch = ~25 retargets/s). **VERIFIED CLEAN (agents traced + refuted):** one-writer rule (saveProfileSoon brain-gates internally), peer reseed determinism, surgery vs skinning/bind matrices, Alt+drag delivery on Windows, zero-default NaN/div-0 sweep, ambient-51 anomaly (NOT reproducible from shipped code — transient session state; fresh load = 452 ✓ live-confirmed). Live battery: mal0_-_v_2_0 + default_h.e.v_suit_km stand sane (no false normalization), stillness default holds. **Latent (logged, not fixed):** name-keyed restAdjust/sprungSet safe on glTF (loader dedupes) but aliasable on FBX dupes; antiparallel thigh (straight-up bind) gets an arbitrary knee facing; static scan tools (describe.mjs) can't resolve live-sanitized override names → avatar_describe reports stale trunk roles for catgirl; double-launch tee collision = silent dead second click (single-instance makes it a no-op anyway).

## ⚡ PER-AVATAR LOG — anime_catgirl ("she is strange", 2026-06-11)
- **ROOT CAUSE: her GLB BINDS IN A ~76° SQUAT** (femur horizontal, knees folded, trunk curled — Sketchfab baked the capture pose; verified offline from raw node transforms: L 75.9° / R 76.5°). Every rest-relative layer built on that crouch → the on-screen "strange" deep-squat hunch. Elbows bind straight (180°) which is why only her lower body read wrong. THE MAL0 LESSON AGAIN (waving bind), other limb.
- **FIX (general, engine-level): bind-pose normalization extended to LEGS + TRUNK** in buildProceduralRig, mirroring aimArm: thigh aimed straight down → shin straight → foot leveled (gated: only when the BIND knee < 150° — standing/A-pose rigs untouched, suite-pinned both ways); trunk leveled at the HIPS (hips→head line → vertical, legs re-aimed after so they can't tilt) + remaining curl unwound through chest→neck→head fractions (gated > ~12° lean). Runs BEFORE flex-axis/FSIGN derivation so axes derive from the standing pose. **VERIFIED LIVE: knee 67°→178°, face visible, upright** (snaps in session 2026-06-11). Benefits any future squat/slouch-bound rip.
- **HAIR + NECK (same day, user: "hair to attach to her head and her neck to not do that"):** ① hair = a CONSEQUENCE of the normalization — hair is authored relative to GRAVITY, so leveling the head rotated her strands into a forward plume (stiff springs) or face-curtains (strong gravity). General fix: buildProceduralRig records every world rotation it applies (`restAdjust`); avatar.js **counter-rotates each sprung chain ROOT** by the net ancestor adjustment and rebuilds the springs → hair hangs exactly as authored while ears/hats correctly follow the head. ② neck kink = the chest role had resolved to **chest_024, a Rigify control STACKED ON the hips** (coincident) → breath/sway/un-slouch rotated the torso at its base and the neck took the correction; re-pointed `chest` → `spine_fk003` (the real upper spine) via rig_overrides. ③ her long thin hair got a per-model spring profile (stiffness .34 / gravity −.8 / drag .62 / breeze .1). **VERIFIED LIVE: hair on the scalp, face clear, smooth neck (snaps 2026-06-11).**
- [x] **Knock-knee/scissor stance — SOLVED 2026-06-11 (see DRAG v2 + BONE ISSUE section):** it was NOT a stance offset — the mesh blended ORG (standing) with stranded squat-bound DEF limb chains; `reparent` surgery moved every DEF limb segment under its ORG twin (arms too — gestures + finger curls now render on her). Live snaps: standing clean, clap works.
- [ ] **Her remaining per-model polish (next pass):** ambient helper soup now 262 (was 452 — DEF limbs reclassified by the surgery; remaining = MCH-/VIS_/tweak_/widget bones — consider skin-influence filtering for ambient + spring: only bones that actually deform verts); **tail sprung as THREE parallel chains** (ORG-tail + tail + DEF-tail × 16 — only DEF- deforms; parent/child double-spring risk on fidget kicks — candidates for the same `reparent` surgery); hands overlap slightly in the rest hang (long arms + center-low anchors — per-model tweak if it bothers); white scarf/chest emblem = her actual outfit (not a bug).

## ⚡ FACIAL v2 — PER-CHANNEL FACE RESOLUTION (audit + research verdict 2026-06-11; user: "use clips as learning data; some avatars have animal/irregular faces; find the best way")
- **AUDIT (tools/audit_faces.py → tools/face_audit.json — binary decode of all 15 models):** **ZERO models have named morphs or VRM** (Sketchfab strips `extras.targetNames`; glTF has no official morph-name field) → the named-morph tier NEVER fires in this library; everything alive today came from the bones tier (lola jaw+32lids, mal0 jaw, roxanne jaw+8lids) or the geometric mouth pick (makiro). RECOVERABLE gold: **shibahu** 51 unnamed morphs + clip "Take 001" → mining gives blink #13 (plus #1/4/6/8 blink-pulses 0.07–0.17s), talk-flutter #15 (18 rapid pulses), sustained-open #16 — #13 is ALSO geometrically eye-region+moves-down = blink CONFIRMED with zero names; **makiro** 76 unnamed + clip drives #18 (makiro IS the old 51dc); **glados** eye-FLAP bones are her eyelids but LID_RE misses "flap" AND bones mode needs a JAW to even reach lids. HARD LIMITS (the models, not us): catgirl + anime_cat_girl = full body, NO face rig; **valkyrie + bunny-robot have NO JOINTS AT ALL (statues — no body anim either)**; frenni/fexa/classic_type_0 = no jaw/lids by design (head subtree fully enumerated: eyes/ears/hair/hat only) → eye-look IS their ceiling; mal0 = no blink channel of any kind (skull face — lore-accurate); marie = names destroyed (structural bone-region scan = only hope).
- **ARCHITECTURE BUGS confirmed:** (1) bones-blink is gated behind jaw existence (`if (jaw)`) — lids-only rigs (glados flaps) can never blink; (2) exclusive modes lose channels (a morph-mouth model with lid bones would lose blink); (3) geom_mouth's single signal ("downward in head region") noise-gates on expression-rich faces — shibahu rejected because MOST of her 17 morphs displace downward.
- **RESEARCH (2 sourced web agents, 2026-06-11):** nobody auto-detects faces on arbitrary models — the consensus ladder = format-native semantics (VRM/descriptor) → BIG name dictionary (ARKit 52 camelCase; Unified Expressions PascalCase incl. `EyeClosedLeft`≠"blink"; `viseme_*` with I/O/U vs ih/oh/ou drift; VRoid `Fcl_*` + mesh-path prefix strip; MMD Japanese + NFKC normalize; CC `V_*`; legacy `vrc.blink_*` + first-4-in-mesh-order) → SYNTHESIS from richer sets (TalkingHead derives visemes from ARKit, MIT three.js; VSeeFace mixes A/I/U/E/O) → bone fallback with CALIBRATED POSES (VRChat jaw-flap = volume-driven stored closed/open rotations; eyelids = stored quaternion states per VRChat descriptor — pose, NOT per-bone semantics) → geometric detection (UNBUILT in OSS — we'd be first) → persisted per-model manual mapping (every serious app's escape hatch: Warudo/Inochi/OLV/VRChat). Blink physiology for clip-mining: close ~105ms / total ~250ms / interval 2–6s. Bonus confidence-gated tier: MediaPipe-as-oracle (render morph@1 vs neutral, diff its own ARKit-52 scores — human-ish faces only, degrades on anime/animal). Even with no mouth, PUBLISH the speech signal (VRChat "Viseme Parameter Only" lesson) so per-model effectors (head-bob, antenna, flaps) can bind.
- [ ] **BUILD (per-channel face resolution):** ① facial.js → per-CHANNEL composition: MOUTH and BLINK resolve independently, each via its own ladder (kills bugs 1+2; lola/mal0/roxanne behavior must be preserved — suite-pin). ② `geom_face.js` — head-anchored EYE-BAND + MOUTH-BAND morph classifier (world-space support centroid + direction + mirrored-twin detection, bands anchored on the resolved head/eye bones; replaces geom_mouth's single signal; fixes shibahu's noise-gate). ③ clip-miner (three.js already parses AnimationClips — morph-track pulse/duty/width stats; blink/talk/expression signatures; cross-validate with ② → agreement = auto-accept). ④ widened name dictionary (cheap; future store-bought models). ⑤ persist the auto-detected face profile per model (rig_overrides `face:{blinkMorphs, mouthMorph, lidPose, confidence}` — the detector WRITES its guesses, the user/AI corrects, manual override wins). ⑥ glados flap-lids override (1 line once ① lands → she blinks her aperture). ⑦ Settings "Face" section (per-channel verdict + morph pickers — fold into the deferred Settings expansion). NOT doing: mal0 fake blink (eye-squash cartoon blink — opt-in someday at most), statues (nothing drivable), animatronic mouths (painted on — eye-look already their ceiling).

## ⚡ PHYSICS SLICE 2 (avatar collider) + EYE-IMPOSTER DEDUPE (2026-06-10; "do as much as you can")
- **SHE'S SOLID NOW**: physics.js `setAvatar` tracks her body as a KINEMATIC CAPSULE (feet→head, centred mid-body, rebuilt only on a meaningful resize else just repositioned via setNextKinematicTranslation each frame). Props bounce off her. New `gesture dropball` (menu Move → "Drop ball on her") drops a baseball onto her → it bounces off her head/shoulders = the visible "she's solid" proof. **LIVE-VERIFIED: snap caught the baseball mid-bounce above makiro's head after rebounding off her capsule.** Wired in animate() (capsule updated each frame from modelDims/sizeScale/pos/_motionY). Test: a ball dropped off-centre onto a kinematic capsule deflects off (rolls to the side, never tunnels) — proves rounded-solid contact. ⚠️ kinematic obstacles need setNextKinematicTranslation EVERY step to stay active (cost a debug cycle). NEXT physics: she catches/holds a ball (hand IK to the ball pos when it's near), sit-on-chair (the gothic chair prop + IK feet/hips), peer prop-mirroring.
- **EYE-IMPOSTER DEDUPE** (scan finding): resolveEyes now excludes eye-look IMPOSTERS — fexa's "Eyepatch" and sexy_roxanne's "Eye_Con" controller both contain "eye" but were getting gaze-driven (a patch rotating, a controller double-applying onto the real eyes). Name filter rejects patch/controller/ctrl/eye_con/aim/target/lookat; AND sided (L/R) eyes now WIN over center candidates (a stray controller that slips through is dropped when real eyes exist; a true single-center-eye rig like aveline/glados keeps its center eye).

## ⚡ MODEL EDITOR IN SETTINGS + RESOLVER "BETWEEN" REPAIR (2026-06-10; user: "what if i had a way to edit said files… the model editor should be in the settings… do as much as you can")
- **RESOLVER TIER 3.5 — structural "between" repair (rig.js `resolveBetween`)**: some rigs name the UPPER ARM "shoulder" (shoulder→elbow→wrist joint naming), so the clavicle won `${side}_shoulder` and the real upper arm was dropped → left_arm/right_arm EMPTY on lola, sexy_roxanne, renamon (the single most-missed role in the library). Fix is naming-AGNOSTIC: the bone sitting BETWEEN the resolved shoulder and forearm in the hierarchy IS the upper arm, anatomically always; same recovers a chest between spine and neck. **LIVE-VERIFIED: lola + sexy_roxanne 16→19 roles** (arm-hang/contrapposto/clasp/clap/IK all light up — confirmed via `query roles`: lola left_arm=arm_left_shoulder_2, chest=spine_middle; sexy_rox left_arm=LeftShoulder_2, chest=Spine2). renamon gains chest (its arm needs an override — "shoulder" matched a FLUFF bone, no clean bridge). Unit-tested (jointStyleArmBiped fixture + resolveBetween isolation + the never-fabricate guard). ⚠️ a running overlay caches rig.js in memory — must RESTART (not just reloadRig) to pick up resolver changes.
- **MODEL EDITOR — in Settings, the user's ruling.** `tools/fix_model.mjs` = a PURE-NODE GLB/glTF patcher (ZERO deps — bone names are JSON strings inside the GLB container; rewriting the JSON chunk + copying the BIN chunk byte-for-byte needs no gltf-transform, no image encoder, no admin). Ops: `renames` (map old→new bone name), `repairMojibake` (re-decode UTF-8-as-Latin1 → restore "Tête"; lost U+FFFD → slug to addressable `Bone_N`). Writes a repaired COPY into models/<id>_fixed/ (original NEVER touched). main.js IPC `avatar:diagnoseModel`/`avatar:repairModel`; preload + avatar.js api; **Settings → "Model repair" section**: shows live role resolution (X/19 + missing list) + bone-name health, a one-click "Fix N broken bone names → new copy", and a per-missing-role bone-picker that renames a chosen bone to a canonical name (`LeftUpperArm` etc.) so the rig resolves. Tested: 4 backend unit tests (rename lands + BIN survives byte-exact; mojibake decoded; U+FFFD slugged; diagnose counts) + 2 jsdom UI tests (repair button → repairModel, role dropdown → rename). **LIVE-PROVEN: repaired marie (205 mojibake names → clean, valid 5.7MB GLB, loads without crash)** — though marie's names are LOST not recoverable, so she resolves 0 roles even clean (geometry can't infer her either) → she's the case that needs MANUAL bone-mapping via the dropdowns (a user maps by sight). The pipeline + UI are the deliverable; full marie recovery is user-driven. v1 LIMITS (backlog): no texture downscale yet (needs an image encoder — renamon's 30MB / roxanne's 71MB textures); FBX not supported (re-export glTF); marie-class manual mapping needs a bone-picker that shows the skeleton + click-to-identify (the dropdowns are name-only today).

## ⚡ PHYSICS ENGINE ADOPTED (rapier) + LIMB-DRAG REMOVED + CHAIN TOGGLES (2026-06-10; user: "the arm thing is too janky lets remove it… chains should show in settings… focus on what you said we should change")
- **LIMB DRAG REMOVED by user ruling ("too janky")** — the IK-chase arm can't feel physical without real dynamics; cleanly stripped (procedural drag mode / setHandTarget / avatar.js grab-detect+body-pull / its test). KEPT: the finger-curl layer + grip (fingers curl when she's carried), the cursor relay, endDrag's publishPos. Re-do hand-leading LATER on top of the physics engine (a proper ragdoll arm), not IK-chase.
- **PHYSICS ENGINE: @dimforge/rapier3d-compat 0.19.3 ADOPTED (§E!)** — npm, portable-node, no admin; WASM inlined → works offline from the importmap. New `physics.js`: lazy world (init on first use), gravity −14, a wide static FLOOR slab kept at the bottom edge of her CURRENT monitor (avatar.js repositions it as she moves), dynamic ball colliders (restitution .62, friction .45, z-translation LOCKED to the screen plane, spin on throw → it rolls). `gesture throwball` (menu Move → "Throw ball", bus, AI) hurls the bundled baseball from her chest toward the cursor's side + a happy emote; "Clear balls" removes them. Adaptive-fps aware (wakes while bodies are awake; sleeps when settled). Suite +`tests/physics.test.js` (headless: falls/bounces/rests ON the line/in-plane). **LIVE-VERIFIED: thrown over the bus, bounced and came to rest at the screen bottom (snap).** v1 LIMITS (backlog): props render in the BRAIN window only (peers need a prop-transform mirror — piggyback the pose-relay pattern); the baseball GLB is 64MB → first throw parses ~seconds (downscale the asset); no avatar collider yet (she can't be hit/catch — needs a capsule + hand interaction); chair/sit + cloth are the next §E slices.
- **CHAIN TOGGLES in Settings** (user: "chains… no way of turning them on or off"): the Jiggle section is now titled "the soft-body bone CHAINS", and every chain row has an explicit ON/OFF checkbox (unchecking remembers the weight and restores it on recheck; slider↔checkbox stay in sync; jsdom-tested). ANSWERED for the record: "chains" = the spring-bone chains (regions); Mal0 has no chain-named accessory bones, and chains have no per-chain MESH — their skin is part of the big body/skin mesh (part 10), which is why the bones view shows them but Parts has no matching entry.
- Also: relayed attachMesh now carries ONE initiator-generated id (a window created mid-session numbered its copies differently → relayed detach silently missed there); Start-Avatar.ps1 tees electron output to %TEMP%\enigma_avatar.log (the quit-reason diagnostics were invisible when launched hidden).

## ⚡ LIMB DRAG + FINGER LIFE (2026-06-10; user: "move her arm/hand by dragging until i drag far enough away that her body comes with… why do the fingers not move?") — **REMOVED same day, see above** (fingers/grip kept)
**LIMB DRAG shipped**: pointerdown near a HAND/FOREARM bone (screen-space proximity, hand wins ties, radius ~6% of her on-screen height) → the arm IK-chases the live cursor (snappy 0.12s spring, slightly in FRONT so it reaches forward not into the torso) while the body stays; past ~92% of arm reach the BODY gets tugged along (60% of the excess/frame = eased). Release → the normal blended release→hang (1s). Reuses the entire pose-spring/IK/blend machinery (`armMode:"drag"` preempts the scheduler; `proc.setHandTarget(side, worldV|null)`). Works from ANY monitor (start/end relay via uiCmd; the brain reads the already-relayed cursor — zero extra move traffic). Grabbing the torso still body-drags as before. **FINGERS**: ambient micro-wiggle was invisible by design → new dedicated COORDINATED curl layer: every finger joint's curl axis+sign CALIBRATED at build (rotate ±X/±Y/±Z, keep what moves the chain tip toward the wrist — the FSIGN trick per joint; leaf joints inherit the parent's axis), slow one-signed open/close drift (~8° deep joints) + **GRIP** — the dragged hand curls while held; a whole-body carry grips BOTH hands (wired off the avatar:pos drag flag; endDrag now publishes the release edge). Mal0: **58 finger joints** calibrated (ambient 129→71 = exactly the 58 moving to the finger layer). Fingers stream to peers via the pose buffer; excluded from ambient (no double-drive); kAmb-blended after gestures. Suite 112/104/0 incl. a limb-drag property test (hand reaches a world target; release < 4.3°/frame). Live-verified: relaunch → `query idle` showed drag mode + left-hand grip active (the user grabbed her hand immediately). USER-EYES: feel of the tug threshold + grip strength; tune `reach*0.92` / grip 0.4 if needed.

## ⚡ AUDIT #3 + MENU-ANYWHERE + CLEANUP (2026-06-10, same day as v4.1; user: "do an audit on what has been done… make right-click work anywhere… thoroughly clean the code")
Three fresh agents (main process / renderer / animation math) + live battery. **THE BIG ONE — the unexplained silent exits, ROOT-CAUSED:** `rebuildWindowSet` passes through a zero-window state for ~60ms on EVERY display event (monitor sleep, DPI/resolution change, taskbar move) → `window-all-closed` fired → **the app quit ITSELF**. Fixed (`_rebuilding` guard + `_quitting` flag). Whole fix batch landed + suite-pinned (111 tests/103 pass/0 fail):
- **MENU/SETTINGS ON ANY MONITOR — DONE.** Every window opens the menu/Settings locally; mutations go through a **UI command relay** (`UI_CMDS` in avatar.js): initiator applies instantly → main re-broadcasts (stamped w/ sender, echo skipped) → scope `"all"` = per-window render state (recolor/hue/mesh-vis/labels/morph/attach/detach/tune/skeleton/shadow/flags/rotate/lookMode/springTune/regionWeight), scope `"brain"` = streams back via pose (loadModel/gesture/express/size/_rotLive). The BUS routes its visual mutations through the same relay → an AI recolor now shows on whatever monitor she's on (**live-proven**: recolor over the bus → snap of the PEER window shows the tint). Peers persist NOTHING (one-writer: brain owns profiles.json/localStorage). Peer windows become focusable only while a panel is open there (Settings text inputs need keys; otherwise they never steal focus). Wheel-resize/hotkeys/drag-drop/rotate-by-drag also work from any window now.
- **Reload brick fixed**: `avatar:init` was sent via `.once(did-finish-load)` → tray "Reload avatar" (or the crash self-heal) left every window permanently blank. Now persistent + re-sends pos/model; tray Reload reloads ALL windows.
- **Drag strands fixed**: rebuild/unexpected window close mid-drag left the 8ms cursor-follow running forever with EVERY window click-through (unrescuable). endDrag on rebuild + a `closed` handler (Alt+F4 on the brain → clean set rebuild) + renderer `pointercancel`/`blur` → dragEnd (Win+L mid-drag). `brainEntry()` filters destroyed windows (in-flight pose IPC used to throw).
- **Glide-vs-drag fight fixed**: avatar:pos now carries a `drag` flag → the brain kills its AI glide when main owns a drag (was rubber-banding 125Hz vs 60Hz).
- **Cursor-look on every monitor**: peers relay pointermove (~30Hz, global DIP) → brain maps into its own px (`cursor.seen` replaces the `x>=0` gate — left-of-primary coords are negative and legit). Live-verified: `query where` shows the cursor from monitor 3.
- **IDLE pops — the audit's P1**: twoBoneIK's ABSOLUTE reach margin (`lab+lcb-0.01`) made the hang-pose hand (99.9% of reach) unreachable → measured **10–19°/frame elbow snap at every arm-pose entry** + the whole release held ~17° over-bent. Now scale-relative (span×0.9995, all epsilons relative). Release→hang handoff gets a **1s blend** (the IK pose differs from hang by ~115° of internal arm TWIST — at 0.3s it read as a twitch; probe-verified 0 oversized steps over 120s). Gesture→gesture switches now blend (`next !== gesture`; clap→jump measured 108°/frame unblended). **Pole twist implemented** (rate-limited 0.05 rad/frame) — the v4.1 "elbows out+forward" poles were inert (only consulted on an exactly-straight chain). swayR noise-dominated (was 62% sine ON THE ROOT). Happy-emote ±17° root pitch → rides spine/chest now (same class as the v4.1 rocking fix). Ambient layer slerps back in after gestures (fingertip snap after long holds). Tests now meter the ARM chain (<4.3°/frame over 120s — the old suite only watched the trunk, which is exactly where these DIDN'T happen).
- **NSFW/region hardening**: ambient skip walks to the nearest classified ANCESTOR (chains named only at the root are now fully excluded — Mal0 ambient 137→129 verified live); region regexes fixed (AnalogStick≠anus, HairKnot/RopeKnot≠dick→hair, Gear/Linear≠ear, LeftEar/Hear=ear kept) + missing vocab added (cock/bulge/vulva/clit/groin/tits/ass — all tested).
- **More renderer fixes**: express/say `dur` NaN-guarded (a `"2s"` from an LLM NaN-poisoned core bone quats with NO self-heal, streamed to peers); furniture no longer ghost-duplicates on model switch (rig-parented attachments now removed+disposed in reapplyAttachments); attach/tune pos/rot/scale sanitized (+ material disposal on detach); blob drag-drop loads commit curKey on SUCCESS only + never broadcast phantom URLs to peers; peer mirror-load failures retry 3× loudly (were: previous model frozen forever, silent); peer `avatar:model` waits for profiles/overrides (race → wrong roles); peers track live `sizeScale` from the pose stream (shadow/grab-box were stale after resize); thumbnails captured only by the window SHE's on (was: every window raced, wrong-space crops overwrote good thumbs — same fix for `snap` rects); brain re-tests hover on the footprint clock (she glided out from under a stationary cursor → stale interactivity ate desktop clicks); display-event cascades coalesce into one trailing rebuild.
- **bus.py origin gate**: rejects browser-origin WebSocket clients at the handshake (None/file://null allowed) — any web page could previously puppet her + read query replies via ws://127.0.0.1:8765.
- **CLEANUP**: profiles.json pruned 25→6 entries (orphaned models, empty no-op objects; all real tuning preserved — verified live: Mal0's weights/labels/rot intact); dead `worldToGlobal` + unused `modelMaterials` api removed; tools/_audit_q1.json probe deleted; models.json synced (+glados, +roxanne_wolf).
- [ ] **Backlog from audit #3 (P3 residue)**: spring.js verlet isn't dt-normalized (hair feel drifts at 15fps rest) nor world-scale-normalized (latent); FPS_REST runs animation time at 75% wall (dt clamp 0.05 vs 1/15 — document or widen); forearm-aim straightens authored 0–44° relaxed binds (only matters between "waving" and "hanging"); hueShift is name-only (recolor moved to index; hue didn't); no waving-bind FIXTURE in tests (live-verified on Mal0 only); idle "repetitive" complaint has no spectral/autocorrelation test; speech has no visible bubble (say = audio+lip-sync only) — bubble would need per-window DOM mirroring.

## ⚡ IDLE v4 — "LIVING LAYERS" (2026-06-10; user: "idle is the most important part and it's no good; keep the IDEA not the code"). 
Two web-research passes (animation craft + 9 real implementations: TalkingHead, Live2D, VMagicMirror, Amica, ChatVRM, IMPROV, NAO, orangeduck springs) → verdict: the v3 STANCE MACHINE was the robotic architecture itself (discrete pose → fixed-duration ease → hold = pops + cadence + dead holds). v4 in procedural.js is a **continuously-moving system**: ① BREATH always on, one-signed sin² @ ~0.27 Hz (raw sine's negative half reads as exhaling below rest), with jittered SIGHS (smooth bell envelope — an instant amplitude jump was a visible 2.9° pop); ② POSTURAL SWAY ~0.7°, two non-harmonic bands + gradient noise, AP > ML, spine counter-rolls (S-curve); ③ WEIGHT SHIFTS = randomized contrapposto DC offsets every ~13s±50% (energy-modulated), chased by **exact critically-damped springs** (`motionmath.dampSpring`, orangeduck closed form) on a per-joint **halflife ladder** (hips 0.4s → chest 0.68 → head 0.85 = follow-through); ④ ARMS hang riding the sway with per-side lag/seeds (asymmetry), occasional IK poses (clasp-front / hand-on-TRUE-hip — anchors now sized from LEG-ROOT spacing, not shoulder width) whose targets spring from wherever the hand IS (entry/release/retarget pop-free by construction) and WANDER while held (moving hold); ⑤ gesture entry/exit slerp-blends ~0.25s (kills the v3 single-frame snap); ⑥ ENERGY noise (minutes-scale) modulates everything (calm↔restless spells); every channel has its own noise seed (`motionmath.noise1/fnoise` — 1D gradient noise, never loops), every timer jittered (`jitter`). Diagnostics: **`query idle`** (armMode/wSide/anchor + hand-target world coords). Tests: tests/idle.test.js — 5 property tests (never dead-still / no pops >0.6°/frame steady-state / shifts both directions / bounded+NaN-free through a gesture interrupt / blended gesture exit) + dampSpring/noise/jitter unit tests. Suite 110/102/0. **VERIFIED LIVE on Mal0: elbow drifts 89.6→92.5→90.1° over 3s (alive, smooth); 3 frames 7s apart = natural hang / square stand / true contrapposto.**
**v4.1 SAME-DAY POLISH (user feedback round):** ① "body/head — and sometimes the WHOLE avatar — rock back and forth" → the AP sway was partly on the HIPS (the rig ROOT — pitching it swings the entire model incl. feet = floating/rocking) and sine-dominated (metronomic). Now: NO pitch sway on the hips (spine/chest only, reduced), noise-dominated mix (sines demoted to texture) → balance wander, not rocking. ② **GROUND SHADOW** (avatar.js): soft radial contact patch at her feet (scene-level; follows global pos in EVERY window incl. peers; stays grounded + fades during jumps = the classic "standing on something" cue; Settings → "Ground shadow" toggle, persisted; she still wanders/drag-moves freely — the shadow follows). ③ Clasp-pose CLIPPING (user: "arms go through the body; light clipping ok"): clasp anchors pushed clear of the body front (sized by max(shoulder, hip×1.3) — hw alone landed INSIDE deep/wide torsos), hand TRAVEL PATH bowed out in front while far from the target (the straight-line spring shortcut cut through the torso), elbow poles out+forward (forearms wrap around, not through). ④ Peer-window right-click now hints "menu on the MAIN monitor / tray" (full menu-anywhere = backlog below). Suite 110/102/0; live-verified incl. user-dragging her to monitor 3 mid-test (rewrite holding) with the shadow rendering on the peer.
- [x] **Menu/Settings on ANY monitor — DONE 2026-06-10** (see AUDIT #3 block above: local menus + the UI command relay).
- [ ] **True foot-planting during weight shifts** — hips ROLL still swings the legs slightly (slow drift, subtle); proper fix = counter-roll the legs by the live hip roll about the world-forward axis (needs an empirical sign check per the abductAxis convention).

**⚠️ CROSS-MODEL DISCOVERY (fixed): Mal0's GLB BINDS IN HER "WAVING" POSE** (left forearm flexed ~97° in the file). `aimArm` normalized only the UPPER arm (sufficient for T-poses where children inherit), so the bent elbow stayed baked into rest[] — the cause of every "hand floating at chest/chin" artifact in v3 (misdiagnosed as anchor sizing twice). Fix: aim the FOREARM segment too (skipped when an elbow already hangs straight → T/A-pose rigs untouched). Elbow at rest: 82.6°→**173.8°**. LESSON: when a "rest" pose looks wrong, check what the model's BIND actually is (`query idle` restWorld vs hand positions) before tuning anything downstream.

**IDLE v3.1 — BODY-SPACE STANCES (2026-06-09; user: "you have not changed the idle… you cannot use x,y,z alone, you have to use the body parts").** Root cause of "looks the same": stance v3's offsets were tiny LOCAL-euler nudges (the exact per-rig-axis trap from the motions). v3.1 drives the PARTS: legs via `flex()` (real body hinges, free knee clearly soft ±0.2rad), HANDS PLACED VIA `twoBoneIK` at body anchors — **on-hip** (elbow-out silhouette) and **clasped-low-front** — stored HIPS-local (pose-invariant, sized by half shoulder width `hw`); targets lerp in world during the eased 1.6s shift so hands sweep hip→front smoothly; emote arm-swings layer ADDITIVELY about the flex axis (pose() would clobber the IK). Torso keeps local pitch/roll (proven) with hip roll up to 0.12rad. VERIFIED: 3 frames 9s apart = three unmistakable silhouettes (arm-out weight shift / both hands clasped front / mirrored). Anchor note: on wide-hipped rigs shoulder-width×1.3 landed outside the hip → 1.05×hw + more forward. Suite 99/91/0.
**FOUR-FIX BATCH (2026-06-09, user: parts toggle inaccessible / missing weights / monitor / "idle deleted and replaced"):** (1) **Parts was auto-COLLAPSING** on models with >8 parts behind a tiny dim header that didn't look clickable — now DEFAULT-OPEN (still collapsible, hover affordance). (2) **Region split**: dick/anus broken out of the lumped "genital" (region.js order: dick→anus→genital; NSFW_REGIONS grew) — Mal0 now shows 8 sliders incl. genital 14 / anus 10 / dick 9 (was one genital 33). (3) **Monitor = TWO-STEP hop** (replaces the failed recomposite nudge): never do a same-size cross-display setBounds — hop SMALL (480×360) onto the target first (different-size cross-display = the path that always worked 1440↔1080), then GROW to full bounds (different-size, same display); drag-hops park the small step UNDER the cursor so the drag survives; `_hopSeq` supersession guard; recoverToPrimary routed through it. ⚠️ NEEDS USER EYES on the twin-1080p pair (the variant that finally has no same-size move anywhere). (4) **IDLE v3 = STANCE MACHINE** — deleted the continuous-noise sway core ("drifting seaweed"); now she HOLDS a believable stance (square / weight-left / weight-right / relaxed half-turn — contrapposto: hip drop, torso counter, head re-level, asymmetric arms, free knee soft) for 6–14s with only breath + glances + wrist micro on top, then EASES (~1.4s) to the next. Verified: frames 10s apart show two distinct held poses. Ambient/fidgets/breeze layers kept. Suite 99/91/0.
**GENERALIZED IDLE (2026-06-09) — "fix the idle; any avatar joins the fray (dog/dragon/spider/spider-girl); move more weights."** Three new layers: (1) **AMBIENT** — every unresolved, unsprung bone gets tiny desynced micro-motion, depth-scaled (`mathutil.ambientAmp`, capped at depth 5 so long chains don't flail; skeleton roots excluded so the model doesn't wobble). Mal0: 19 driven bones → **175** (19 roles + 156 ambient: fingers/face/extras); GLaDOS: 2 → **44** (segments breathe). A spider/dragon with NO humanoid roles now reads alive. Excludes sprung bones + eye bones via `proc.bindExtras` (wired in avatar.js after springs/eyes resolve). Runs ONLY in the full-idle branch — gestures/motions/clips keep controlled stillness. Tunable: `tune({ambient:0})`. (2) **FIDGETS** — `spring.impulse(region, {x,y,z}, dur)` kicks a region's verlet tips (works WITH the physics; swings + settles; pinned regions immune; NSFW regions never auto-kicked); idle scheduler fires tail-swish / ear-flick / wing-ruffle / hair-toss every 7–16s on whatever regions the model HAS. New bus action `impulse` (AI body language: wag on demand) — live-verified on Mal0's tail. (3) **WEIGHT-SHIFT** — slow (~12s) hip drop onto one leg with torso counter-lean + knee softening (`params.shift`) — the main "standing alive, not swaying jelly" cue. Tests: spring impulse physics test (kick deflects tail, hair untouched, settles back) + ambientAmp; suite **97/89/0**.
**"MOVE MORE BONES" amplitude pass (same day, user ask):** breeze now reaches ALL dangly chains, not just geo-fallback ones (the ~100 sprung hair/tail/ear bones sat DEAD at rest — the single biggest statue tell; per-region `BREEZE_SCALE`, NSFW regions get NO wind; default breeze 0.16→0.22; regression test added). Ambient gained a SLOW 8s posture-drift band (more amplitude at LOW freq — never shake) + base 0.02→0.03; finger bones (hand descendants) boosted ×2.6; role idle livelier (armSwing .085→.105, look .17, sway .05, shift .065, wrist .06); fidgets every 5–12s (was 7–16). VERIFIED: two idle frames 2.6s apart differ clearly (head turn, hair resettle, tail swept aside, weight shift). Suite 99/91/0.
**AUDIT of the above (same day, skeptical agent — verdict: implementation sound, 11 suspicions cleared) + fixes landed:** impulse paths now gated on `springOn` (fidgets/bus kept QUEUEING while springs were toggled off → re-enabling fired a whip-burst); `impulse()` rejects a user-pinned region (weight 0) so the fidget falls through to another appendage instead of landing invisibly; facial-owned jaw/lid bones excluded from ambient via new `facial.boneNames()` (they'd tremble with facial off) — verified live: Mal0 ambient 156→155 = exactly the jaw. Also: **rotate-mode now auto-disarms when Settings closes** (user ask; jsdom-tested) · **Mal0 variants documented + round-trip verified** (§A2: mesh 9 suit / mesh 10 skin; no morphs) · clothes-jiggle answered (suit is skinned to body bones → follows breast/butt weights; independent cloth = §E physics engine). Suite **98/90/0**.
look-at fixed (cursorIdle reset) · eye-look + head/eyes/both toggle · **head-look vertical reversal fixed** · idle eye-darts · **idle suspended during gestures** · gesture framework (animate-through-bones) · **rig study tool** (analyze_rig.py) · AI spatial awareness (screen-px in status + `goTo` anchors + `where`) · move-on-import (relocate to models/) · rename = local label (files untouched) · **monitor recomposite nudge** · multi-mesh morph (primary group) · 3-axis rotation + drag-spin · jiggle regions + NSFW per-area weights · morphs-by-index + auto-label · adaptive frame rate (60/30/15) · backgroundThrottling:false · cloth-weight UI · model gallery + thumbnails + trash + rename · settings-stays-open-on-avatar-click · audit #7 (all 4 invariants PASS) · **AUDIT-FIX 2026-06-09 (trust-nothing): move-on-import made TRANSACTIONAL (no partial-delete data loss) + dialog=copy / drop=move · rotation no longer clobbers a running motion (setRot/spinLive guarded, _motion cleared on model-switch) · idle+eye-look+darts suspended during gesture/motion (true stillness) · capture filename basename'd · regionFeel confirmed bit-identical to the old spring math.**
· **MOTIONS NOW FULLY ANIMATED (skeletal, not root-only) 2026-06-09:** jump/flip/laydown/getup ARTICULATE the body via synced bone clips in procedural.js (`jumpPose`/`flipPose`/`lyingCurl`), driven through the new `flex()` hinge-axis primitive (limbs bend on the body's real left→right hinge, NOT broken local pitch). Jump = coil(crouch)→launch→air-tuck→absorb + a front-readable vertical squash-and-stretch dip in avatar.js `updateMotion`. Flip = 360° tumble + cannonball tuck. Lay-down = curl onto her side (knees up, ~78° recline not a stiff plank) and HOLD (gestureHold) with a faint breath; get-up unfolds + un-tips from the recorded `_lying` state. Idle/eyes suspended during a motion; a model-switch / manual-rotate `_cancelMotion()` clears motion+lying+clip. **Verified live (front + side capture + numeric joints):** knee 177°→~110° on the crouch, lay-down holds curled, eye-gaze x flips L↔R. Bug found+fixed mid-session: the return-object `setGesture` ignored its `opts` arg → `gestureHold` never set → lay-down never held (idle resumed). Bone clips also run now even if the idle toggle is OFF (animate-loop condition widened to `idleOn || inMotion || gesturing()`).
· **MOTION TRIGGERS + CROSS-MODEL HARDENING + POLISH + TESTS 2026-06-09:** right-click **"Move"** submenu (Jump/Flip/Lay down/Get up/Clap) + **Ctrl+Alt+J / Ctrl+Alt+F** hotkeys, so the USER can fire motions (they were bus-only → untestable). `FSIGN` now **AUTO-DETECTED** (forward = up×right, toe-disambiguated; +flex calibrated to bend toward forward) → motions bend the right way on a rig facing ANY direction (makiro auto-resolves to +1, fwd≈+Z — live-verified via `query joints`). Jump height + dips **SCALE** with the model's visible height (`modelDims.h × sizeScale`) → consistent on any model/size. Polish: landing squash (deeper knee absorb), arm follow-through (overshoot settle), crouch **knee-SPLAY** (abduction via a new `abductAxis`) so the foreshortened crouch reads head-on. Pure motion math extracted to **`motionmath.js`** (`bell`/`easeInOut`/`jumpElevation`) — procedural.js + avatar.js now IMPORT it (the tested code IS the shipped code) — **+5 unit tests** (dip-before-apex, apex≈h, scales-with-h, settles). **Tests 94 / 0-fail.** Live re-verified: fsign=1, the polished jump dips→springs cleanly front-on.
