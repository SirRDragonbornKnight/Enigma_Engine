# Enigma AI Engine Environment Configuration
# ==========================================
# Copy this file to .env and fill in your values.
# These are optional - Enigma AI Engine works fully offline without API keys.

# === ENIGMA API KEY (for securing YOUR API server) ===
# Set this to require authentication when running the API server
# Generate a random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
# ENIGMA_API_KEY=your-secure-key-here

# === DIRECTORY OVERRIDES ===
# Uncomment to use custom directories
# FORGE_DATA_DIR=/path/to/data
# FORGE_MODELS_DIR=/path/to/models
# FORGE_MEMORY_DIR=/path/to/memory

# === DEVICE OVERRIDE ===
# Options: auto, cpu, cuda, mps
# FORGE_DEVICE=auto

# === API SERVER ===
# FORGE_API_HOST=127.0.0.1
# FORGE_API_PORT=5000

# === LOGGING ===
# Options: DEBUG, INFO, WARNING, ERROR
# FORGE_LOG_LEVEL=INFO

# === EXTERNAL API KEYS (Optional - for cloud AI providers) ===
# These are only needed if you want to use external APIs instead of local models

# OpenAI (for image generation, code completion)
# OPENAI_API_KEY=sk-...

# Replicate (for image, video, audio generation)
# REPLICATE_API_TOKEN=r8_...

# ElevenLabs (for voice synthesis)
# ELEVENLABS_API_KEY=...

# HuggingFace (for downloading HF models)
# HF_TOKEN=hf_...

# === NGROK (for exposing your API to the internet) ===
# NGROK_AUTHTOKEN=...
